FROM ghcr.io/kafbat/kafka-ui:latest

LABEL authors="dannguyenmessi"

ENV KAFKA_CLUSTERS_0_NAME=kafka
ENV KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS=kafka:29092
ENV KAFKA_CLUSTERS_0_METRICS_PORT=9997
ENV DYNAMIC_CONFIG_ENABLED=true

EXPOSE 8080