server:
  port: 8080

spring:
  application:
    name: users
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  profiles:
    active: "dev"
  config:
    import: optional:configserver:http://localhost:8071
  servlet:
    multipart:
      max-file-size: 25MB
      max-request-size: 30MB

springdoc:
  api-docs:
    path: /users/api-docs
  swagger-ui:
    path: /users/swagger-ui

oauth:
  openId:
    url: http://localhost:7080/realms/forum/.well-known/openid-configuration

keycloak:
  management-user:
    realm: forum
    server: http://localhost:7080
    client-id: admin-cli
    grant-type: password
    name: didannguyen
    password: 17052002
    client-secret: l2WiWQULnEFt9KrgySvFK03QtVf9K4DO

app:
  cache:
    redis:
      enabled: true
      host: 'redis://redis-12610.c252.ap-southeast-1-1.ec2.redns.redis-cloud.com'
      port: 12610
      timeoutSeconds: 60
      maxPoolSize: 8
      maxIdle: 8
      minIdle: 0
      password: "jfz74hpkdMFx6qTfjIZT4kOixxq9iFB6"

management:
  endpoints:
    web:
      exposure:
        include: "*"
  health:
    readinessstate:
      enabled: true
    livenessstate:
      enabled: true
  endpoint:
    health:
      probes:
        enabled: true
    shutdown:
      enabled: true
  info:
    env:
      enabled: true
  metrics:
    tags:
      application: ${spring.application.name}

endpoints:
  shutdown:
    enabled: true

info:
  app:
    name: ${spring.application.name}
    description: "Forum users application"
    version: "1.0.0"

logging:
  level:
    com:
      didan:
        forum:
          users: debug
  pattern:
    level: "%5p [${spring.application.name},%X{trace_id},%X{span_id}]"

minio:
  access-key: IFxmZQZnzwfeigv79Lo3
  secret-key: M64UOvH5OAFZx7q4V1fGi6BbOmH9PXaAtfuyW9x3
  endpoint: http://localhost:10000
  bucket-name: materials